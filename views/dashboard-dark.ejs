<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
  <meta charset="UTF-8">
  <title>لوحة التحكم الليلية - مع فلاتر</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.rtl.min.css">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">
  <style>
    body {
      background-color: #1b1b2f;
      color: #e0e0e0;
      font-family: 'Segoe UI', sans-serif;
    }
    .navbar-dark {
      background-color: #111;
    }
    .sidebar {
      height: 100vh;
      background-color: #12121c;
      padding: 20px;
      position: fixed;
      top: 56px;
      right: 0;
      width: 220px;
    }
    .sidebar a {
      color: #ccc;
      display: block;
      margin: 12px 0;
      text-decoration: none;
    }
    .sidebar a:hover {
      color: #fff;
    }
    .main-content {
      margin-right: 240px;
      padding: 30px;
      margin-top: 56px;
    }
    .card-dark {
      background-color: #2e2e42;
      border: none;
      border-radius: 12px;
    }
    .filter-label {
      font-weight: bold;
      margin-bottom: 5px;
    }
    h2, h5 {
      color: #f8f9fa;
    }
  </style>
</head>
<body>
<nav class="navbar navbar-expand-lg navbar-dark fixed-top">
  <div class="container-fluid">
    <a class="navbar-brand" href="/">👤 منصة تقييم الاحتياجات</a>
    <div class="d-flex">
      <a href="/" class="btn btn-sm btn-outline-light me-2">الصفحة الرئيسية</a>
      <a href="/auth/logout" class="btn btn-sm btn-outline-danger">تسجيل الخروج</a>
    </div>
  </div>
</nav>
<div class="sidebar">
  <h5 class="text-white">📋 القائمة</h5>
  <a href="#stats"><i class="bi bi-bar-chart"></i> إحصاءات عامة</a>
  <a href="#export"><i class="bi bi-download"></i> تصدير البيانات</a>
  <a href="#questions"><i class="bi bi-ui-checks"></i> إدارة الأسئلة</a>
  <a href="#team"><i class="bi bi-people"></i> إدارة الفريق</a>
  <a href="#account"><i class="bi bi-person-circle"></i> الحساب</a>
</div>
<div class="main-content">
  <section id="stats">
    <h2 class="mb-4">📊 إحصاءات عامة</h2>
    <div class="row g-3 mb-4">
      <div class="col-md-3">
        <label class="filter-label">المنظمة</label>
        <select id="filterOrganization" class="form-select form-select-sm"><option value="">الكل</option></select>
      </div>
      <div class="col-md-3">
        <label class="filter-label">رمز المنظمة</label>
        <input type="text" id="filterCode" class="form-control form-control-sm" placeholder="مثال: ORG-1">
      </div>
      <div class="col-md-3">
        <label class="filter-label">الفئة العمرية</label>
        <select id="filterAge" class="form-select form-select-sm">
          <option value="">الكل</option>
          <option value="6-12">6-12</option>
          <option value="13-18">13-18</option>
          <option value=">18">أكبر من 18</option>
        </select>
      </div>
      <div class="col-md-3">
        <label class="filter-label">هل تلقى تدريب سابق؟</label>
        <select id="filterTraining" class="form-select form-select-sm">
          <option value="">الكل</option>
          <option value="نعم">نعم</option>
          <option value="لا">لا</option>
        </select>
      </div>
    </div>
    <div class="row">
      <div class="col-md-4">
        <div class="card card-dark p-3 mb-4 text-center">
          <h5>عدد المشاركين</h5>
          <h3 id="totalParticipants">--</h3>
        </div>
      </div>
      <div class="col-md-4">
        <div class="card card-dark p-3 mb-4 text-center">
          <h5>عدد المنظمات</h5>
          <h3 id="totalOrganizations">--</h3>
        </div>
      </div>
      <div class="col-md-4">
        <div class="card card-dark p-3 mb-4 text-center">
          <h5>نسبة المتدربين سابقًا</h5>
          <h3 id="previousTrainingPercentage">--</h3>
        </div>
      </div>
    </div>
    <div class="card card-dark p-4 mb-4">
      <canvas id="domainsChart"></canvas>
    </div>
  </section>

  <section id="export" class="mt-5">
    <h2>📝 تصدير البيانات</h2>
    <button class="btn btn-outline-light" onclick="exportToExcel()">📥 تصدير إلى Excel</button>
  </section>

  <section id="questions" class="mt-5">
    <h2>🧩 إدارة الأسئلة</h2>
    <p class="text-muted">سيتم تطوير واجهة تفاعلية لإدارة أسئلة الاستبيان.</p>
  </section>

  <section id="team" class="mt-5">
    <h2>👥 إدارة الفريق</h2>
    <p class="text-muted">إضافة وتعديل أعضاء الفريق لاحقًا.</p>
  </section>

  <section id="account" class="mt-5">
    <h2>⚙️ إعدادات الحساب</h2>
    <p class="text-muted">تغيير كلمة المرور وخيارات الخروج.</p>
  </section>
</div>
<script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/xlsx/0.18.5/xlsx.full.min.js"></script>
<script src="/js/dashboard-dark.js"></script>
</body>
</html>
