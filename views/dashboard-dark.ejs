<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>لوحة التحكم | Orphans Care</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.rtl.min.css" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" rel="stylesheet">
  <style>
    body {
      background-color: #1e1e2f;
      color: #f0f0f0;
    }
    .navbar {
      background-color: #12121c;
    }
    .card-dark {
      background-color: #2c2c3d;
      border: none;
      border-radius: 10px;
      box-shadow: 0 0 10px rgba(0,0,0,0.4);
    }
    .card-dark h5 {
      color: #aaa;
    }
    .chart-container {
      background-color: #2c2c3d;
      border-radius: 10px;
      padding: 20px;
      margin-bottom: 30px;
    }
  </style>
</head>
<body>
  <!-- Navbar -->
  <nav class="navbar navbar-expand-lg navbar-dark">
    <div class="container-fluid">
      <a class="navbar-brand text-light" href="/">منصة رعاية الأيتام</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav ms-auto">
          <li class="nav-item"><a class="nav-link text-light" href="/">🏠 الصفحة الرئيسية</a></li>
          <li class="nav-item"><a class="nav-link text-light" href="/auth/logout">🚪 تسجيل الخروج</a></li>
        </ul>
      </div>
    </div>
  </nav>

  <!-- محتوى اللوحة -->
  <div class="container py-4">
    <h2 class="mb-4">📊 لوحة التحكم</h2>

    <!-- Cards -->
    <div class="row g-3 mb-4">
      <div class="col-md-3">
        <div class="card card-dark text-center p-3">
          <h5>عدد المشاركين</h5>
          <h2 id="totalParticipants">--</h2>
        </div>
      </div>
      <div class="col-md-3">
        <div class="card card-dark text-center p-3">
          <h5>عدد المنظمات</h5>
          <h2 id="totalOrganizations">--</h2>
        </div>
      </div>
      <div class="col-md-3">
        <div class="card card-dark text-center p-3">
          <h5>نسبة المتدربين سابقًا</h5>
          <h2 id="previousTrainingPercentage">--</h2>
        </div>
      </div>
      <div class="col-md-3">
        <div class="card card-dark text-center p-3">
          <h5>أعلى مجال احتياجًا</h5>
          <h2 id="highestNeedDomain">--</h2>
        </div>
      </div>
    </div>

    <!-- Charts -->
    <div class="chart-container">
      <h5 class="text-center">📈 توزيع المجالات</h5>
      <canvas id="domainsChart"></canvas>
    </div>

    <div class="chart-container">
      <h5 class="text-center">🎯 توزيع الخبرات</h5>
      <canvas id="experienceChart"></canvas>
    </div>

    <!-- تصدير البيانات -->
    <div class="text-end mt-4">
      <button class="btn btn-outline-light" onclick="exportToExcel()">
        <i class="bi bi-file-earmark-arrow-down"></i> تصدير إلى Excel
      </button>
    </div>
  </div>

  <!-- المكتبات -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/xlsx/0.18.5/xlsx.full.min.js"></script>
  <script src="/js/dashboard-dark.js" defer></script>
</body>
</html>
