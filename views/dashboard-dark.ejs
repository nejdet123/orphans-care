<!-- لوحة التحكم مع فلاتر متقدمة - نسخة داكنة -->
<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
  <meta charset="UTF-8">
  <title>لوحة التحكم الليلية - مع فلاتر</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.rtl.min.css">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">
  <style>
    body {
      background-color: #1e1e2f;
      color: #f0f0f0;
    }
    .sidebar {
      height: 100vh;
      background-color: #12121c;
      padding: 20px;
      position: fixed;
      top: 0;
      right: 0;
      width: 240px;
    }
    .sidebar a { color: #bbb; display: block; margin: 15px 0; text-decoration: none; }
    .sidebar a:hover { color: #fff; }
    .main-content { margin-right: 260px; padding: 30px; }
    .card-dark { background-color: #2c2c3d; border: none; }
    .filter-label { font-weight: bold; margin-bottom: 5px; }
  </style>
</head>
<body>
<div class="sidebar">
  <h4 class="mb-4">لوحة التحكم</h4>
  <a href="#stats"><i class="bi bi-bar-chart-line"></i> إحصاءات عامة</a>
  <a href="#export"><i class="bi bi-file-earmark-arrow-down"></i> تصدير البيانات</a>
  <a href="#questions"><i class="bi bi-ui-checks"></i> إدارة الأسئلة</a>
  <a href="#team"><i class="bi bi-people"></i> إدارة الفريق</a>
  <a href="#account"><i class="bi bi-person-circle"></i> إعدادات الحساب</a>
</div>
<div class="main-content">
  <section id="stats">
    <h2 class="mb-3">📊 إحصاءات عامة</h2>

    <!-- فلاتر التقرير -->
    <div class="row g-3 mb-4">
      <div class="col-md-3">
        <label class="filter-label">المنظمة</label>
        <select id="filterOrganization" class="form-select form-select-sm"></select>
      </div>
      <div class="col-md-3">
        <label class="filter-label">رمز المنظمة</label>
        <input type="text" id="filterCode" class="form-control form-control-sm">
      </div>
      <div class="col-md-3">
        <label class="filter-label">الفئة العمرية</label>
        <select id="filterAge" class="form-select form-select-sm">
          <option value="">الكل</option>
          <option value="6-12">6-12</option>
          <option value="13-18">13-18</option>
          <option value=">18">أكبر من 18</option>
        </select>
      </div>
      <div class="col-md-3">
        <label class="filter-label">هل تلقى تدريب سابق؟</label>
        <select id="filterTraining" class="form-select form-select-sm">
          <option value="">الكل</option>
          <option value="نعم">نعم</option>
          <option value="لا">لا</option>
        </select>
      </div>
    </div>

    <!-- كروت الإحصاءات -->
    <div class="row">
      <div class="col-md-4"><div class="card card-dark p-3 mb-3"><h5>عدد المشاركين</h5><h3 id="totalParticipants">--</h3></div></div>
      <div class="col-md-4"><div class="card card-dark p-3 mb-3"><h5>عدد المنظمات</h5><h3 id="totalOrganizations">--</h3></div></div>
      <div class="col-md-4"><div class="card card-dark p-3 mb-3"><h5>نسبة المتدربين سابقًا</h5><h3 id="previousTrainingPercentage">--</h3></div></div>
    </div>

    <!-- رسوم بيانية -->
    <div class="card card-dark p-4 mb-4">
      <canvas id="domainsChart"></canvas>
    </div>
  </section>
  <section id="export" class="mt-5">
    <h2>📝 تصدير البيانات</h2>
    <button class="btn btn-outline-light me-2" onclick="exportToExcel()">📥 Excel</button>
  </section>
  <section id="questions" class="mt-5">
    <h2>🧩 إدارة الأسئلة</h2>
    <p>سيتم تطوير واجهة مخصصة هنا لتعديل كامل الاستبيان.</p>
  </section>
  <section id="team" class="mt-5">
    <h2>👥 إدارة الفريق</h2>
    <p>إضافة وتعديل أعضاء الفريق لاحقًا.</p>
  </section>
  <section id="account" class="mt-5">
    <h2>⚙️ إعدادات الحساب</h2>
    <p>خيارات تغيير كلمة المرور وتسجيل الخروج.</p>
  </section>
</div>
<script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/xlsx/0.18.5/xlsx.full.min.js"></script>
<script src="/js/dashboard-dark.js"></script>
</body>
</html>
