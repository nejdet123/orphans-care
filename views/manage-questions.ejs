<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
  <meta charset="UTF-8" />
  <title>إدارة الأسئلة</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.rtl.min.css">
  <style>
    body { background-color: #f5f5f5; padding: 2rem; }
    .section-block { margin-bottom: 2rem; background: #fff; padding: 1.5rem; border-radius: 8px; box-shadow: 0 2px 6px rgba(0,0,0,0.1); }
    .subsection-title { color: #0d6efd; margin-top: 1.5rem; }
    .question-box { padding: 0.75rem; background: #f8f9fa; border-radius: 5px; margin-bottom: 0.5rem; }
    .question-actions { font-size: 0.9rem; }
  </style>
</head>
<body>
  <div class="container">
    <h1 class="mb-4 text-primary">🧾 إدارة أسئلة الاستبيان</h1>

    <% if (!survey) { %>
      <div class="alert alert-warning">لا يوجد استبيان متاح في الوقت الحالي.</div>
    <% } else { %>
      <% survey.sections.forEach((section, sectionIndex) => { %>
        <div class="section-block">
          <h3><%= section.title %></h3>

          <% section.subsections?.forEach((sub, subIndex) => { %>
            <div class="subsection">
              <h5 class="subsection-title">📌 <%= sub.title %></h5>

              <% sub.questions?.forEach((q, qIndex) => { %>
                <div class="question-box">
                  <strong><%= q.number %>. <%= q.text %></strong>
                  <div class="question-actions text-muted">
                    النوع: <%= q.type %> | مطلوب؟ <%= q.required ? "نعم" : "لا" %>
                    <!-- مستقبلاً: إضافة أزرار تعديل/حذف -->
                  </div>
                </div>
              <% }) %>
            </div>
          <% }) %>
        </div>
      <% }) %>
    <% } %>
  </div>
</body>
</html>
