<%- include('../layout') %>

<div class="container mt-4">
  <h2>📝 إدارة الأسئلة</h2>

  <!-- إضافة سؤال جديد -->
  <form action="/api/questions" method="POST" class="mb-4">
    <div class="row g-2">
      <div class="col-md-8">
        <input name="question" class="form-control" placeholder="اكتب نص السؤال الجديد" required>
      </div>
      <div class="col-md-4">
        <button type="submit" class="btn btn-primary w-100">➕ إضافة سؤال</button>
      </div>
    </div>
  </form>

  <hr>

  <!-- عرض وتعديل الأسئلة -->
  <% if (questions && questions.length > 0) { %>
    <% questions.forEach((q, i) => { %>
      <form action="/api/questions/<%= i %>?_method=PUT" method="POST" class="mb-2 d-flex gap-2">
        <input name="newQuestion" value="<%= q %>" class="form-control">
        <button type="submit" class="btn btn-success">💾 حفظ</button>
        <form action="/api/questions/<%= i %>?_method=DELETE" method="POST">
          <button type="submit" class="btn btn-danger">🗑️</button>
        </form>
      </form>
    <% }) %>
  <% } else { %>
    <p>🚫 لا توجد أسئلة مضافة بعد.</p>
  <% } %>
</div>
