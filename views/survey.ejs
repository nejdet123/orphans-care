<!DOCTYPE html>
<html lang="ar">
<head>
  <meta charset="UTF-8">
  <title>استبيان تقييم الاحتياجات التدريبية</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.rtl.min.css" rel="stylesheet">
  <style>
    body { direction: rtl; text-align: right; padding: 2rem; background-color: #f9f9f9; }
    .question-block { margin-bottom: 20px; }
  </style>
</head>
<body>
  <div class="container">
    <h2 class="text-center mb-4">استبيان تقييم الاحتياجات التدريبية</h2>

    <% if (questions && questions.length > 0) { %>
      <form action="/submit-survey" method="POST">
        <% questions.forEach((q, i) => { %>
          <div class="question-block">
            <label class="form-label"><%= q %></label>
            <select name="answers[]" class="form-select" required>
              <option value="">اختر التقييم</option>
              <option value="1">1 - ضعيف جدًا</option>
              <option value="2">2 - ضعيف</option>
              <option value="3">3 - متوسط</option>
              <option value="4">4 - جيد</option>
              <option value="5">5 - ممتاز</option>
            </select>
          </div>
        <% }) %>
        <button type="submit" class="btn btn-primary mt-3">إرسال الاستبيان</button>
      </form>
    <% } else { %>
      <p class="text-danger text-center">لا توجد أسئلة متاحة حالياً.</p>
    <% } %>
  </div>
</body>
</html>
