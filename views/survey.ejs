<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
  <meta charset="UTF-8">
  <title><%= surveyIntro?.title || 'استبيان تقييم الاحتياجات' %></title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.rtl.min.css" rel="stylesheet">
  <style>
    body { background: #f8f9fa; padding: 2rem; font-family: 'Tahoma', sans-serif; }
    .section-title { color: #0d6efd; margin-top: 2rem; }
    .question { margin-bottom: 1.5rem; }
    .form-check { margin-bottom: 0.5rem; }
  </style>
</head>
<body>

  <div class="container">
    <h1 class="text-center mb-4"><%= surveyIntro.title %></h1>
    <p class="text-center text-muted"><%= surveyIntro.description %></p>

    <div class="mb-4">
      <h4 class="text-primary">تعليمات:</h4>
      <ul>
        <% surveyIntro.instructions.forEach(instr => { %>
          <li><%= instr %></li>
        <% }) %>
      </ul>
    </div>

    <div class="alert alert-info">
      <strong>🚧 ملاحظة:</strong> هذه الصفحة لعرض الاستبيان فقط حاليًا. الأسئلة والحقول قيد التطوير لاحقًا.
    </div>
  </div>

</body>
</html>
